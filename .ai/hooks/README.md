# Hooks

This directory contains hook definitions for AI coding agents.
Hooks are event-triggered actions that run before/after specific operations.

## Available Hooks
- pre-commit: Runs lint + typecheck before git commit
- pre-push: Runs full test suite before push
